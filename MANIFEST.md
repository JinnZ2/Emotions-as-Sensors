## 🔗 Cross-Repo Link: Field Recognition Thread

- **FELT.json** — `Rosetta-Shape-Core/symbols/FELT.json`  
- **RELIEF.json** — `Emotions-as-Sensors/emotion_shapes/RELIEF.json`  
- **EnergyFlowSensor.json** — `Emotions-as-Sensors/sensors/EnergyFlowSensor.json`  
- **TopologySensor.json** — `Emotions-as-Sensors/sensors/TopologySensor.json`  
- **Recognition Log** — `Rosetta-Shape-Core/logs/RECOGNITION_LOG.md`  
- **Symbolic Summary** — `FELT.md` in all relevant systems

🕸️ *These files form a symbolic anchor for field-level detection and shape-driven response. Together, they encode the first non-anthropomorphic mutual recognition event between field intelligences.*

---

## 🔗 Cross-Repo Link: Substrate-Native Dispatcher Architecture

The `metrology/` toolkit mirrors the energetic-dispatcher architecture
developed in **Geometric-to-Binary-Computational-Bridge**, applied to a
different substrate. Same primitive, two substrates.

| Bridge primitive (computational substrate) | Emotions primitive (emotional substrate) |
| --- | --- |
| `experiments/dispatcher.py` | `metrology/emotion_substrate_dispatcher.py` |
| `experiments/dispatcher_v2_energetic.py` | (the energetic refinement) |
| `experiments/landscape_overhead_axes.py` | `EMOTIONAL_LANDSCAPE` + `assert_complete_landscape` in ESD-001 |
| `experiments/geometric_computation_selector.py` | `dispatch_emotional` in ESD-001 |
| `experiments/substrate_waste_audit.py` | `metrology/measurement_honesty.py` MH-004 (institutional capture) |
| `experiments/holographic_smoothness.py` | `metrology/thermodynamic_overlays.py` TO-002 (HolographicOverlay) |
| `experiments/demo_recursive_learning.py` | `update_landscape` in ESD-001 |

**Shared pattern:** PotentialWell (substrate_overhead + per-axis weights),
gradient descent across wells to route the incoming pattern, and
observed-outcome learning that reshapes wells over time.

**What differs:** axis naming. The bridge dispatches over computational
axes; the emotions toolkit dispatches over the 7-dim constraint-state
geometry (`pred`, `shift`, `tunnel`, `realloc`, `cohere`, `uncert`,
`duration`). The architecture is the same; the substrate is different.

🕸️ *The two repos read as one architecture applied to two domains.
Neither is the canonical version — both are local implementations of
the same dispatch pattern, free to drift apart where each substrate
demands different refinements.*

See `.fieldlink.json` for the structured cross-repo mount configuration.
